# SRAM-Pico2W
Two-layer firmware for RP2350: Arduino (or any, really) apps run entirely from SRAM, kernel stays in flash. Syscall interface provides controlled hardware access. Enables rapid development cycles.
